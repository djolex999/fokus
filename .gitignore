node_modules
dist
src-tauri/target
.DS_Store
CLAUDE.local.md

# generated by tauri at build time
src-tauri/gen/schemas

# Audio lives in ~/fokus/audio, outside the project. This entry exists because
# the project directory is also called fokus, so dropping tracks in the wrong
# one is a single wrong turn, and an audio file is tens of megabytes.
audio/
